---
import { getCollection } from 'astro:content'

const posts = await getCollection('posts')
---
<ul>
  {posts.map(post => (
    <li>
      <a href={`/posts/${post.slug}`}>{post.data.title}</a>
    </li>
  ))}
</ul>


<!-- <section class="w-screen h-screen flex items-center justify-center">Navbar</section>
<section class="w-screen h-screen flex items-center justify-center">Hero section</section>
<section class="w-screen h-screen flex items-center justify-center">Category sections</section>
<section class="w-screen h-screen flex items-center justify-center">Why landmarks</section>
<section class="w-screen h-screen flex items-center justify-center">Compare</section>
<section class="w-screen h-screen flex items-center justify-center">Treatments</section>
<section class="w-screen h-screen flex items-center justify-center">Footer</section> -->